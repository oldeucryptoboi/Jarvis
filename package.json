{
  "name": "jarvis-monorepo",
  "private": true,
  "description": "Jarvis — Deterministic agent runtime with explicit plans, typed tools, permissions, and replay",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "clean": "pnpm -r clean",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@jarvis/api": "workspace:^",
    "@jarvis/browser-relay": "workspace:^",
    "@jarvis/cli": "workspace:^",
    "@jarvis/journal": "workspace:^",
    "@jarvis/kernel": "workspace:^",
    "@jarvis/permissions": "workspace:^",
    "@jarvis/planner": "workspace:^",
    "@jarvis/plugins": "workspace:^",
    "@jarvis/schemas": "workspace:^",
    "@jarvis/tools": "workspace:^",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.0",
    "uuid": "^11.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
